<% if logged_in? %>
  <p>ログイン完了ユーザ：<%= current_user.name %></p>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the Tasklists</h1>
      <h2>あなたのタスクを管理しよう！</h2>
      <br>
      <%= link_to 'タスク管理を開始する！',  signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>